---
"@formwright/schema": minor
"@formwright/core": minor
"@formwright/dom": minor
---

Initial release: schema types + runtime validator, the signal-reactive core and
`Form` class, and the surgical virtual-DOM-free renderer with core widgets.
