---
"@formwright/overlay-schema": minor
"@formwright/overlay-core": minor
---

Add Overlaywright to npm: `@formwright/overlay-schema` and `@formwright/overlay-core` for schema-driven modals, drawers, and sheets.
