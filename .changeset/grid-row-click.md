---
"@formwright/grid-dom": patch
---

Gridwright now stamps `data-row-id` on every data row, copies column `class` onto header cells, and accepts `onRowClick` on `mount` (clicks on checkboxes, expanders, buttons, and `[data-gw-interactive]` are ignored).
