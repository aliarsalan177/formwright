---
"@formwright/ui": minor
---

Dates and commands: `<fw-calendar>` (single or range, one or two months, min/max and disabled dates, locale week start, full grid keyboard), `<fw-date-picker>` (typed or picked, range mode submitting `start/end`) and `<fw-command-palette>` with `<fw-command>` and `<fw-command-group>` (a hotkey such as `mod+k`, ranked filtering).
